/**
 * Required by expo-router alongside `map.native.tsx` / `map.web.tsx`.
 * Bundler picks `.native` / `.web` where available; this file satisfies the fallback route slot.
 */
export { default } from './map.web';
