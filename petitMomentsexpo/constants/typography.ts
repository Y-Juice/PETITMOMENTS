/**
 * Loaded in root `_layout` via `useFonts`. Use these `fontFamily` names in styles.
 * - Fraunces: display / titles
 * - Glacial Indifference: body copy and UI text
 */
export const FontFamily = {
  titleBold: 'Fraunces_700Bold',
  titleRegular: 'Fraunces_400Regular',
  body: 'GlacialIndifference-Regular',
} as const;
